
import { featuredIn } from 'thisdot/helpers/featured-in';
import { module, test } from 'qunit';

module('Unit | Helper | featured in');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = featuredIn([42]);
  assert.ok(result);
});

